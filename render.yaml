services:
  - type: web
    name: lumina-updates
    env: python
    pythonVersion: "3.11.0"
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: ADMIN_KEY
        value: Lucas2024Secret!
