services:
  - type: web
    name: lumina-updates
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: ADMIN_KEY
        value: LUCAS_ADMIN_SECRET_2024
